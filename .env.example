# =========================================
# BI SaaS - Environment Variables
# =========================================
# Copy this file to .env.local and fill in the values
# NEVER commit .env.local to version control
# =========================================

# ------------------------------------------
# Supabase Configuration (Required)
# ------------------------------------------

# Supabase Project URL
# Found in: Supabase Dashboard -> Settings -> API -> Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anonymous Key (Public)
# Found in: Supabase Dashboard -> Settings -> API -> anon public
# This key is safe to expose in browser
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase Service Role Key (Secret - Server Only)
# Found in: Supabase Dashboard -> Settings -> API -> service_role
# WARNING: This key bypasses Row Level Security (RLS)
# NEVER expose this key in client-side code
# NEVER use ANON_KEY as fallback for this variable
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# -----------------------------------------
# Application Configuration (Required)
# -----------------------------------------

# Application URL
# Used for redirects, OAuth callbacks, and link generation
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------
# Multi-Tenant Configuration
# -----------------------------------------

# List of allowed tenant schemas (for reference)
# Actual validation is done in src/lib/security/validate-schema.ts
# Current schemas: okilao, saoluiz, paraiso, lucia, sol

# -----------------------------------------
# Security Notes
# -----------------------------------------

# 1. SUPABASE_SERVICE_ROLE_KEY:
#    - Has full access to all data (bypasses RLS)
#    - Only use on server-side (API routes, server actions)
#    - If compromised, regenerate immediately in Supabase Dashboard
#
# 2. NEXT_PUBLIC_* variables:
#    - Are exposed to the browser
#    - Should only contain non-sensitive configuration
#
# 3. Production checklist:
#    - [ ] All keys are production keys (not development)
#    - [ ] SERVICE_ROLE_KEY is set and not using ANON_KEY fallback
#    - [ ] APP_URL points to production domain
#    - [ ] .env.local is in .gitignore
